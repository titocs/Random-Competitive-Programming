#include <bits/stdc++.h>
using namespace std;

int main(){
    int hasil, x, y, z;
    cin >> x >> y >> z;
    if(x + y + z > 200)
        cout << "Lolos";
    else
        cout << "Tidak lolos";
    return 0;
}
