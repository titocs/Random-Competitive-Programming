#include <bits/stdc++.h>
using namespace std;

int main(){
    int hasil, x, y, z;
	  cin >> x >> y >> z;
	  hasil = x + y + z;
	  if(hasil > 200)
		    printf("Lolos");
	  else
		    printf("Tidak Lolos");
	  return 0;
}
